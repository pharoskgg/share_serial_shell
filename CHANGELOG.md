## 0.4.0

- 新增“协作终端: 一键接入 Claude CLI 和桌面应用”：安全合并用户级 MCP 配置，复用稳定 stdio 桥接器，保留其他 Claude 设置并拒绝覆盖同名用户配置。
- MCP 常用工具从 10 个收敛到 4 个；管理操作统一为 `session(action)`，默认精简、分页读取，`raw` 保留完整字节。
- 串口按 4 字节 / 6ms 节流，人工与 AI 共享有界 FIFO；断开或发送失败取消后续输入。
- Agent 默认读取增加 120ms 分包收集窗口，连续输出合并到约 8 KiB；raw 模式仍保留完整字节，支持 0–1000ms 的 `settleMs` 调整。
- Agent 读取会把终端逐键产生的人工输入及其交错回显聚合到回车/换行边界，不因输入速度拆分，并跨源事件分页完成当前命令；UI、导出和底层串口发送仍保留原始时序。
- 强化 MCP 服务与桥接器的工具说明：用户明确说共享终端、串口或 SSH 时优先复用共享会话；终端只保留设备正常回显，AI 输入单独记录。
- 原生终端内直接显示 AI 输入记录，不依赖设备回显；控制字符安全转义，保留原始审计记录。
- 支持工作区 / WSL 远程宿主路径和配置，修复 macOS PTY helper 执行权限，增加分平台 CI 与跨 Node 版本测试入口。
- 工具调用等待上限增至 180 秒，容纳节流发送。升级后需要刷新 MCP 工具列表；旧管理工具改用 `session`。

# 更新记录

## 0.3.1

- 修复 AI 每次写入串口时自动从原生终端跳转到协作终端面板的问题。已有会话的读写不再调用显示界面的逻辑。
- 保留用户当前标签和键盘焦点，输入记录与后台收发照常工作。
- 明确 MCP 与桥接器的工具说明：仅在用户要求显示会话时调用 `show_session`。

## 0.3.0

- 插件激活即自动注册 VS Code MCP、本机 Codex 和项目连接文件，取消手动启用步骤。
- 内置稳定的 stdio 桥接入口，使用 VS Code 自带运行时；无需安装 Node、复制令牌或固定端口。
- 自动发现多个 VS Code 窗口，按会话 UUID 转发；窗口端口和令牌变化后自动恢复连接。
- 保留用户其他 Codex 配置，注册幂等、并发写入串行化、文件原子替换。
- 状态栏区分等待 AI、AI 已访问和接入异常；提供“一键修复 AI 接入”。
- 首次注册不能强行刷新已运行的外部 Agent 对话；状态提示需要客户端加载工具，不自动中断终端。
- 增加无窗口启动、迟到窗口、多窗口路由、配置保留、实际 stdio 进程和自动启动集成测试。

## 0.2.1

- 修复串口视图 focus 命令未注册时打开失败：已有视图直接显示，首次打开支持从底部容器恢复。
- 更新后界面未完成注册时，提供“重新加载窗口”恢复按钮。
- 修复串口监视器、全部会话及 AI 连接命令的中文名称。
- 增加 focus 命令缺失回归测试，以及实际安装 VSIX 后的面板打开、隐藏与重开测试。

## 0.2.0

- 串口改用底部固定监视器，取消顶部多步弹窗。
- 串口下拉选择、厂商信息、刷新和手动设备路径；无设备时明确提示。
- 同屏波特率、高级参数、连接、断开、文本 / HEX 收发和行尾设置。
- 设备、人工、AI 收发记录分别标记；时间戳、自动滚动、清空显示、会话切换。
- 监视器、原生串口终端与 MCP 共用会话。

## 0.1.1

- 一键准备当前项目的本机 AI 连接配置，并在窗口重载时更新。

## 0.1.0

- 原生 SSH、串口和本地终端，MCP HTTP 服务与共享输入记录。
